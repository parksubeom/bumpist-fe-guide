# Reproducible, strict installs across the team.
engine-strict=true
# Fail fast on phantom dependencies instead of relying on a hoisted node_modules.
# (pnpm default, made explicit so no one loosens it accidentally.)
shamefully-hoist=false
